/**
* Memory OS class developed in project 12
*
* I had to include this class because the Java implementation has seemingly a bug and crashed with Stack Overflow
*/
class Memory {
    static Array _MEMORY;
    static Array _ALLOCATOR;

    static int x;

    /** Initializes the class. */
    function void init() {
        let _MEMORY = 0;

        let _ALLOCATOR = 2048;
        let _ALLOCATOR[0] = 0;
        let _ALLOCATOR[1] = 14335;

        return;
    }

    /** Returns the RAM value at the given address. */
    function int peek(int address) {
        return _MEMORY[address];
    }

    /** Sets the RAM value at the given address to the given value. */
    function void poke(int address, int value) {
        let _MEMORY[address] = value;
        return;
    }

    /** Finds an available RAM block of the given size and returns
     *  a reference to its base address. */
    function int alloc(int size) {
        var Array q;

        let x = x + 1;

        while ((_ALLOCATOR[1] + 2 < size)) {
            let _ALLOCATOR = _ALLOCATOR[0];
            if (_ALLOCATOR = 0) {
                // Out of memory
                do Sys.error(1000);
            }
        }
        let q = _ALLOCATOR + _ALLOCATOR[1] - size;
        let q[-1] = size;

        let _ALLOCATOR[1] = _ALLOCATOR[1] - size - 2;
        let _ALLOCATOR = 2048;
        return q;
    }

    /** De-allocates the given object (cast as an array) by making
     *  it available for future allocations. */
    function void deAlloc(Array o) {
        var int next;
        let next = _ALLOCATOR[0];
        let _ALLOCATOR[0] = o - 2;
        let o[0] = next;
        return;
    }
}
